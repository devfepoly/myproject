<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Laptopshop - Quên mật khẩu</title>
    <link rel="stylesheet" href="/stylesheets/output.css" />
    <link rel="stylesheet" href="/stylesheets/slider.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css">

</head>

<body class="min-h-screen bg-cover bg-center"
    style="background-image: url('/asset/images/backgrounds/reset-password.jpg')">
    <main>
        <div class="flex items-center justify-center min-h-screen">

            <div class="w-full max-w-md p-8 space-y-6 bg-white rounded-xl shadow-lg">

                <div class="text-center">
                    <h1 class="text-3xl font-bold text-gray-900">
                        Lấy lại mật khẩu
                    </h1>
                    <p class="mt-2 text-gray-600">
                        Vui lòng nhập email của bạn.
                    </p>
                </div>

                <form method="POST" action="" class="space-y-6">
                    <% if (errors && errors.forgotPassword) { %>
                        <div class="mb-4">
                            <ul class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded">
                                <% errors.forgotPassword.forEach(function(error) { %>
                                    <li>
                                        <%= error %>
                                    </li>
                                    <% }); %>
                            </ul>
                        </div>
                        <% } %>
                    <% if (successes && successes.forgotPassword) { %>
                        <div class="mb-4">
                            <ul class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded">
                                <% successes.forgotPassword.forEach(function(suc) { %>
                                    <li>
                                        <%= suc %>
                                    </li>
                                    <% }); %>
                            </ul>
                        </div>
                        <% } %>

                        <div>
                            <input type="text" name="email" id="email" placeholder="Nhập email của bạn" required
                                class="w-full px-3 py-2 mt-1 border border-gray-300 rounded-md shadow-sm focus:ring-indigo-500 focus:border-indigo-500">
                        </div>
                        <button class="py-3 w-full font-semibold bg-cellphones-500 hover:bg-cellphones-700 text-white rounded-lg cursor-pointer">
                            <i class="fa-solid fa-arrow-right"></i>
                            Xác nhận
                        </button>
                </form>
            </div>
        </div>
    </main>
</body>

</html>