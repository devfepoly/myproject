<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>LAPTOPSHOP - Đăng ký</title>
    <link rel="stylesheet" href="/stylesheets/output.css" />
    <link rel="stylesheet" href="/stylesheets/slider.css" />
</head>

<body class="bg-cover bg-center" style="background-image: url('/asset/images/backgrounds/register.jpg');">
    <main class="py-10 min-h-fit min-w-full flex justify-center items-center">
        <div
            class="py-7 px-10 lg:px-20 md:min-w-[75%] lg:min-w-1/3 min-w-[90%] bg-white text-black rounded-2xl shadow-lg opacity-95">
            <h1 class="text-3xl font-bold mb-6 text-center">Đăng ký</h1>

            <!-- Help Text for Validation Errors -->
            <% if (typeof errors !=='undefined' && errors.length> 0 && typeof old !== 'undefined') { %>
                <div class="mb-4">
                    <ul class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded">
                        <% errors.forEach(function(error) { %>
                            <li>
                                <%= error %>
                            </li>
                            <% }) %>
                    </ul>
                </div>
                <% } %>

                    <form action="register" method="POST" class="space-y-6">
                        <div>
                            <label for="name" class="block text-sm font-medium mb-2">Họ và tên <span
                                    class="text-red-500">*</span></label>
                            <input type="text" id="name" name="name" required
                                value="<%= typeof old !== 'undefined' && old.name ? old.name : '' %>"
                                class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-cellphones-500" />
                        </div>
                        <div>
                            <label for="email" class="block text-sm font-medium mb-2">Email <span
                                    class="text-red-500">*</span></label>
                            <input type="email" id="email" name="email" required
                                value="<%= typeof old !== 'undefined' && old.email ? old.email : '' %>"
                                class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-cellphones-500" />
                        </div>
                        <div>
                            <label for="phone" class="block text-sm font-medium mb-2">Số điện thoại</label>
                            <input type="text" id="phone" name="phone"
                                value="<%= typeof old !== 'undefined' && old.phone ? old.phone : '' %>"
                                class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-cellphones-500" />
                        </div>
                        <div>
                            <label for="password" class="block text-sm font-medium mb-2">Mật khẩu <span
                                    class="text-red-500">*</span></label>
                            <input type="password" id="password" name="password" required
                                class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-cellphones-500" />
                        </div>
                        <div>
                            <label for="confirm_password" class="block text-sm font-medium mb-2">Nhập lại mật khẩu <span
                                    class="text-red-500">*</span></label>
                            <input type="password" id="confirm_password" name="confirm_password" required
                                class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-cellphones-500" />
                        </div>
                        <button
                            class="w-full bg-gradient-to-b from-cellphones-300 to-cellphones-500 hover:from-cellphones-600 hover:to-cellphones-800 text-white py-2 rounded-md transition duration-300 cursor-pointer"
                            type="submit">
                            Đăng ký
                        </button>
                    </form>
                    <div class="mt-6 flex flex-col items-center gap-2">
                        <span class="text-sm">Đã có tài khoản?</span>
                        <a href="login"
                            class="inline-block bg-cellphones-500 hover:bg-cellphones-600 text-white px-4 py-2 rounded-md font-semibold transition duration-300">Đăng
                            nhập</a>
                        <a href="/" class="inline-block mt-2 text-cellphones-500 hover:underline text-sm">Quay về trang
                            chủ</a>
                    </div>
        </div>
    </main>
</body>
</html>