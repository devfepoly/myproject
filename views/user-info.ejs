<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>LAPTOPSHOP - Thông tin tài khoản
    </title>
    <link rel="stylesheet" href="/stylesheets/output.css" />
    <link rel="stylesheet" href="/stylesheets/slider.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css">
</head>

<body>
    <%- include("./partials/header.ejs") %>

        <main class="pt-36 py-30 px-5 lg:px-20 text-black grid grid-cols-12 gap-5 bg-gray-200">
            <section
                class="px-5 py-5 lg:px-8 lg:py-10 w-full h-fit col-span-12 grid grid-cols-12 gap-2 lg:gap-0 bg-white rounded-2xl">
                <div
                    class="col-span-12 lg:col-span-4 flex justify-start items-center gap-5 border-0 lg:border-r-4 lg:border-cellphones-500">
                    <div
                        class="p-2 flex justify-center items-center bg-gradient-to-b from-cellphones-50 to-cellphones-100 w-fit h-fit rounded-full">
                        <img src="/asset/images/icons/avata-ant.b574f3e9.svg" alt="User Avatar"
                            class="w-10 h-10 rounded-full object-cover">
                    </div>
                    <div class="h-full flex flex-col justify-start items-start">
                        <h2 class="font-semibold text-xl">
                            <%= user.name.length> 20 ? user.name.substring(0, 20) + "..." : user.name %>
                        </h2>
                        <p class="text-sm text-gray-500">
                            <%= user.email %>
                        </p>
                        <div class="w-full text-sm text-gray-500 flex gap-2">
                            <p id="profile-phone-number"></p>
                            <button class="cursor-pointer" id="btn-toggle-profile-phone-number"></button>
                        </div>
                    </div>
                </div>
                <div
                    class="p-0 lg:px-4 col-span-6 lg:col-span-4 flex justify-start items-center gap-5 border-0 lg:border-r-4 lg:border-cellphones-500">
                    <div
                        class="p-2 flex justify-center items-center bg-gradient-to-b from-cellphones-50 to-cellphones-100 w-fit h-fit rounded-full">
                        <img src="/asset/images/icons/cart-icon.3e4e1d83.svg" alt="Cart Icon"
                            class="w-5 h-5 md:w-10 md:h-10 rounded-full object-cover">
                    </div>
                    <div class="flex flex-col justify-start items-start">
                        <h2 class="text-xl lg:text-2xl font-semibold">
                            <%= totalOrders %>
                        </h2>
                        <p class="text-sm text-gray-500">Tổng số đơn hàng đã mua</p>
                    </div>
                </div>
                <div class="p-0 lg:px-4 col-span-6 lg:col-span-4 flex justify-start items-center gap-5 border-0">
                    <div
                        class="p-2 flex justify-center items-center bg-gradient-to-b from-cellphones-50 to-cellphones-100 w-fit h-fit rounded-full">
                        <img src="/asset/images/icons/money-icon.3e6b67af.svg" alt="Money Icon"
                            class="w-5 h-5 md:w-10 md:h-10 rounded-full object-cover">
                    </div>
                    <div class="flex flex-col justify-start items-start">
                        <h2 id="grand-total-orders" class="text-xl lg:text-2xl font-semibold">
                            <%= grandTotalOrders %>đ
                        </h2>
                        <p class="text-sm text-gray-500">Tổng tiền tích lũy</p>
                        <div class="p-1 bg-gray-50 hidden lg:block">
                            <p class="text-xs">Hãy mua nhiều hơn để nhận được nhiều <b>ưu đãi</b>.</p>
                        </div>
                    </div>
                </div>
            </section>

            <section class="py-5 w-full h-fit hidden md:block md:col-span-4 lg:col-span-3 bg-white rounded-2xl">
                <a href="/user"
                    class="py-5 px-7 w-full flex justify-start items-center gap-3 hover:bg-cellphones-50 text-lg hover:text-cellphones-500 font-semibold hover:border-l-4 hover:border-cellphones-600">
                    <i class="fa-regular fa-envelope-open"></i>
                    Tổng quan
                </a>
                <a href="/order"
                    class="py-5 px-7 w-full flex justify-start items-center gap-3 hover:bg-cellphones-50 text-lg hover:text-cellphones-500 font-semibold hover:border-l-4 hover:border-cellphones-600">
                    <i class="fa-regular fa-clipboard"></i>
                    Lịch sử mua hàng
                </a>
                <a href=""
                    class="py-5 px-7 w-full flex justify-start items-center gap-3 bg-cellphones-50 text-lg text-cellphones-500 font-semibold border-l-4 border-cellphones-600">
                    <i class="fa-regular fa-user"></i>
                    Thông tin tài khoản
                </a>
                <a href="/warranty"
                    class="py-5 px-7 w-full flex justify-start items-center gap-3 hover:bg-cellphones-50 text-lg hover:text-cellphones-500 font-semibold hover:border-l-4 hover:border-cellphones-600">
                    <i class="fa-regular fa-circle-check"></i>
                    Chính sách bảo hành
                </a>
                <a href="/support"
                    class="py-5 px-7 w-full flex justify-start items-center gap-3 hover:bg-cellphones-50 text-lg hover:text-cellphones-500 font-semibold hover:border-l-4 hover:border-cellphones-600">
                    <i class="fa-regular fa-envelope"></i>
                    Góp ý - Phản hồi - Hỗ trợ
                </a>
                <form action="/auth/logout" method="POST">
                    <button
                        class="py-5 px-7 w-full flex justify-start items-center gap-3 hover:bg-cellphones-50 text-lg hover:text-cellphones-500 font-semibold hover:border-l-4 hover:border-cellphones-600 border-t-2 border-gray-200 cursor-pointer">
                        <i class="fa-solid fa-arrow-right-from-bracket"></i>
                        Đăng xuất
                    </button>
                </form>
            </section>

            <section class="w-full h-fit col-span-12 md:col-span-8 lg:col-span-9 grid grid-cols-12 gap-3">
                <!-- Note -->
                <% if (!user.gender) { %>
                    <div
                        class="px-5 py-4 col-span-12 text-sm lg:text-[16px] flex justify-between items-center gap-4 bg-blue-50 text-blue-500 border-2 border-blue-300 rounded-2xl">
                        <i class="fa-solid fa-triangle-exclamation"></i>
                        <p class="flex-1 text-black">Vui lòng cập nhật giới tính để có trải nghiệm tốt hơn.</p>
                        <a href="user/user-info" class="font-semibold">Cập nhật</a>
                    </div>
                <% } %>

                <!-- Profile -->
                <div class="p-4 col-span-12 grid grid-cols-12 bg-white rounded-2xl">
                    <div class="col-span-12 flex justify-between items-center">
                        <h3 class="font-semibold">Thông tin cá nhân</h3>
                        <button id="btn-open-edit-profile"
                            class="text-cellphones-500 font-semibold hover:underline cursor-pointer">
                            <i class="mr-1 fa-regular fa-pen-to-square"></i>
                            Cập nhật
                        </button>
                    </div>
                    <div class="mt-5 col-span-12 grid grid-cols-12 gap-0 lg:gap-7">
                        <div class="col-span-12 lg:col-span-6 flex flex-col">
                            <div
                                class="py-4 w-full flex justify-between items-center text-sm border-b-[1px] border-gray-300">
                                <p class="font-semibold text-gray-500">Họ và tên:</p>
                                <p class="font-semibold">
                                    <%= user.name %>
                                </p>
                            </div>
                            <div
                                class="py-4 w-full flex justify-between items-center text-sm border-b-[1px] border-gray-300">
                                <p class="font-semibold text-gray-500">Giới tính:</p>
                                <p class="font-semibold">
                                    <% if (!user.gender) { %>
                                        -
                                        <% } else if (user.gender==='male' ) { %>
                                            Nam
                                            <% } else if (user.gender==='female' ) { %>
                                                Nữ
                                                <% } else { %>
                                                    Khác
                                                    <% } %>
                                </p>
                            </div>
                            <div
                                class="py-4 w-full flex justify-between items-center text-sm border-b-[1px] border-gray-300 xl:border-b-0">
                                <p class="font-semibold text-gray-500">Số điện thoại:</p>
                                <p class="font-semibold">
                                    <%= user.phone ? user.phone : "-" %>
                                </p>
                            </div>
                        </div>
                        <div class="col-span-12 lg:col-span-6 flex flex-col">
                            <div
                                class="py-4 w-full flex justify-between items-center text-sm border-b-[1px] border-gray-300">
                                <p class="font-semibold text-gray-500">Email:</p>
                                <p class="font-semibold">
                                    <%= user.email %>
                                </p>
                            </div>
                            <div
                                class="py-4 w-full flex justify-between items-center text-sm border-b-[1px] border-gray-300">
                                <p class="font-semibold text-gray-500">Địa chỉ mặc định:</p>
                                <p class="font-semibold">
                                    <% let defaultAddress=addresses.find(addr=> addr.is_default);
                                        if (defaultAddress) { %>
                                        <%= defaultAddress.street %>, <%= defaultAddress.ward %>, <%= defaultAddress.district %>, <%= defaultAddress.city %>
                                        <% } else { %>
                                            -
                                    <% } %>
                                </p>
                            </div>
                            <div class="py-4 w-full flex justify-between items-center text-sm">
                                <p class="font-semibold text-gray-500">Lần cập nhật gần nhất:</p>
                                <p class="font-semibold" id="last-update">
                                    <%= user.updatedAt %>
                                </p>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Address -->
                <div class="p-4 col-span-12 grid grid-cols-12 gap-4 bg-white rounded-2xl">
                    <div class="col-span-12 flex justify-between items-center">
                        <h3 class="font-semibold">Sổ địa chỉ</h3>
                        <button id="btn-open-add-address"
                            class="text-cellphones-500 font-semibold hover:underline cursor-pointer">
                            <i class="mr-1 fa-solid fa-plus"></i>
                            Cập nhật
                        </button>
                    </div>
                    <div
                        class="mt-5 py-7 col-span-12 grid grid-cols-12 gap-0 lg:gap-7 max-h-[500px] overflow-auto">
                        <% if (addresses.length===0) { %>
                            <div class="col-span-12">
                                <img src="/asset/images/icons/empty.f8088c4d.png" alt="empty"
                                    class="m-auto w-[140px] h-[104px] rounded-full object-cover">
                                <p class="mt-2 text-center text-sm text-gray-500">
                                    Bạn chưa có địa chỉ nào được tạo.
                                </p>
                            </div>
                        <% } else { %>
                            <div class="col-span-12 grid grid-cols-12 gap-3">
                                <% addresses.forEach(addr=> { %>
                                    <div class="p-3 col-span-12 xl:col-span-6 bg-gray-50 rounded-lg flex flex-col justify-start items-start">
                                        <div class="w-full flex justify-between items-center">
                                            <p class="text-sm"><%= addr.recipient_name %> | <%= addr.recipient_phone %></p>
                                            <% if (addr.is_default) { %>
                                                <span class="p-1 text-xs text-green-950 bg-green-300 font-semibold rounded-xs">Mặc định</span>
                                            <% } %>
                                        </div>
                                        <div>
                                            <p class="mt-2 text-sm text-gray-500"><%= addr.street %>, <%= addr.ward %>, <%= addr.district %>, <%= addr.city %></p>
                                        </div>
                                        <div class="w-full flex justify-end gap-5">
                                            <form action="/address/delete/<%= addr._id %>?_method=DELETE" method="POST">
                                                <button class="mt-2 px-4 py-1 bg-blue-300 hover:bg-blue-500 hover:text-white cursor-pointer rounded-md">Xóa</button>
                                            </form>
                                            <% if (addr.is_default) { %>
                                                <form action="/address/update/<%= addr._id %>?_method=PATCH" method="POST">
                                                    <input type="hidden" name="is_default" value="off">
                                                    <button
                                                        class="mt-2 px-4 py-1 bg-cellphones-400 hover:bg-cellphones-600 text-yellow-950 hover:text-white cursor-pointer rounded-md">
                                                        Xóa mặc định
                                                    </button>
                                                </form>
                                                <% } else { %>
                                                    <form action="/address/update/<%= addr._id %>?_method=PATCH" method="POST">
                                                        <input type="hidden" name="is_default" value="on">
                                                        <button
                                                            class="mt-2 px-4 py-1 bg-green-300 hover:bg-green-500 text-green-950 hover:text-white cursor-pointer rounded-md">
                                                            Đặt mặc định
                                                        </button>
                                                    </form>
                                                    <% } %>
                                        </div>
                                    </div>
                                <% }) %>
                            </div>
                        <% } %>
                    </div>
                </div>

                <!-- Change password -->
                <div class="p-4 col-span-12 grid grid-cols-12 gap-4 bg-white rounded-2xl">
                    <div class="col-span-12 flex justify-between items-center">
                        <h3 class="font-semibold">Mật khẩu</h3>
                        <button id="btn-open-change-password" class="text-cellphones-500 font-semibold hover:underline cursor-pointer">
                            <i class="mr-1 fa-regular fa-pen-to-square"></i>
                            Thay đổi mật khẩu
                        </button>
                    </div>
                
                    <form action="/auth/change-password?_method=PATCH" method="POST"
                        class="col-span-12 space-y-5 flex flex-col justify-start items-start">
                        <div class="w-full">
                            <label for="old_password" class="block text-sm font-medium text-gray-700 mb-1">
                                Mật khẩu cũ <span class="text-red-500">*</span>
                            </label>
                            <input type="password" id="old_password" name="old_password" required
                                class="w-full border border-gray-300 rounded-lg px-3 py-2 focus:outline-none focus:ring-2 focus:ring-cellphones-500"
                                placeholder="Nhập mật khẩu cũ của bạn">
                        </div>
                
                        <div class="w-full">
                            <label for="new_password" class="block text-sm font-medium text-gray-700 mb-1">
                                Mật khẩu mới <span class="text-red-500">*</span>
                            </label>
                            <input type="password" id="new_password" name="new_password" required
                                class="w-full border border-gray-300 rounded-lg px-3 py-2 focus:outline-none focus:ring-2 focus:ring-cellphones-500"
                                placeholder="Nhập mật khẩu mới của bạn">
                        </div>
                
                        <div class="w-full">
                            <label for="confirm_new_password" class="block text-sm font-medium text-gray-700 mb-1">
                                Nhập lại mật khẩu <span class="text-red-500">*</span>
                            </label>
                            <input type="password" id="confirm_new_password" name="confirm_new_password" required
                                class="w-full border border-gray-300 rounded-lg px-3 py-2 focus:outline-none focus:ring-2 focus:ring-cellphones-500"
                                placeholder="Nhập lại mật khẩu">
                        </div>
                
                        <button
                            class="ml-auto px-5 py-1 bg-cellphones-400 hover:bg-cellphones-700 text-white font-semibold cursor-pointer rounded-md">
                            Cập nhật
                        </button>
                    </form>
                
                    <% if (errors && errors.changePassword) { %>
                        <p class="col-span-12 text-sm text-red-500 font-semibold">
                            <%= errors.changePassword[0] %>
                        </p>
                    <% } %>
                
                    <% if (successes && successes.changePassword) { %>
                        <p class="col-span-12 text-sm text-green-600 font-semibold">
                            Đổi mật khẩu thành công!
                        </p>
                    <% } %>

                    <form action="/auth/forgot-password" method="POST" class="col-span-12 flex justify-end items-center">
                        <input type="text" hidden name="email" value=<%= user.email %>>
                        <p class="">Bạn quên mật khẩu? <button type="submit" class="px-3 py-2 bg-green-600 hover:bg-green-700 text-black hover:text-white rounded-lg cursor-pointer">Đặt lại</button> mật khẩu ngay.</p>
                    </form>

                    <% if (errors && errors.forgotPassword) { %>
                        <p class="col-span-12 text-sm text-red-500 font-semibold">
                            <%= errors.forgotPassword[0] %>
                        </p>
                    <% } %>
                
                    <% if (successes && successes.forgotPassword) { %>
                        <p class="col-span-12 text-sm text-green-600 font-semibold">
                            <%= successes.forgotPassword[0] %>
                        </p>
                    <% } %>
                
                    <p class="col-span-12 text-sm text-gray-500">
                        Hãy cập nhật mật khẩu thường xuyên để an toàn hơn.
                    </p>    
                </div>

            </section>
        </main>

        <!-- Menu Mobile -->
        <section
            class="px-20 pb-5 pt-1 fixed bottom-0 left-0 right-0 z-50 flex justify-between items-center md:hidden bg-white rounded-t-2xl"
            style="box-shadow: 0 -8px 24px 0 rgba(0,0,0,0.12), 0 -1.5px 4px 0 rgba(0,0,0,0.10);">
            <a href="/" class="pt-2 flex flex-col items-center">
                <i class="fa-solid fa-house text-xl"></i>
                <span class="text-xs">Trang chủ</span>
            </a>
            <a href="/order" class="pt-2 flex flex-col items-center">
                <i class="fa-regular fa-clipboard text-xl"></i>
                <span class="text-xs">Đơn hàng</span>
            </a>
            <a href="" class="pt-2 flex flex-col items-center text-cellphones-700 border-t-4 border-cellphones-600">
                <i class="fa-regular fa-user text-xl"></i>
                <span class="text-xs">Tài khoản</span>
            </a>
            <form action="/auth/logout" method="POST">
                <button type="submit" class="pt-2 flex flex-col items-center">
                    <i class="fa-solid fa-arrow-right-from-bracket text-xl"></i>
                    <span class="text-xs">Đăng xuất</span>
                </button>
            </form>
        </section>

        <!-- Dialog Edit Profile -->
        <div id="dialog-edit-profile" class="fixed inset-0 z-50 overflow-y-auto hidden">
            <!-- Backdrop -->
            <div id="backdrop-edit-profile" class="absolute inset-0 bg-black opacity-20 transition-opacity"></div>

            <!-- Content -->
            <div
                class="py-4 px-10 absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 flex flex-col items-start justify-start gap-5 min-h-fit min-w-[400px] md:min-w-[500px] xl:min-w-[650px] bg-white rounded-2xl">
                <!-- Header -->
                <div class="p-4 w-full">
                    <h3 id="dialogTitle" class="text-xl text-center font-semibold text-gray-800">Cập nhật thông tin
                        cá nhân</h3>
                </div>

                <!-- Body -->
                <form action="/user/update?_method=PATCH" method="POST"
                    class="w-full space-y-5 flex flex-col justify-start items-start">
                    <input type="hidden" name="_method" value="PATCH">

                    <!-- Name -->
                    <div class="w-full">
                        <label for="name" class="block text-sm font-medium text-gray-700 mb-1">Họ và tên <span
                                class="text-red-500">*</span></label>
                        <input type="text" id="name" name="name" value="<%= user.name %>" required
                            class="w-full border border-gray-300 rounded-lg px-3 py-2 focus:outline-none focus:ring-2 focus:ring-cellphones-500"
                            placeholder="Nhập họ và tên của bạn">
                    </div>

                    <!-- Gender -->
                    <div class="w-full">
                        <label for="gender" class="block text-sm font-medium text-gray-700 mb-1">Giới tính</label>
                        <select id="gender" name="gender"
                            class="w-full border border-gray-300 rounded-lg px-3 py-2 focus:outline-none focus:ring-2 focus:ring-cellphones-500 bg-white">
                            <option value="" <%=!user.gender ? 'selected' : '' %>>Chưa được cập nhật</option>
                            <option value="male" <%=user.gender==='male' ? 'selected' : '' %>>Nam</option>
                            <option value="female" <%=user.gender==='female' ? 'selected' : '' %>>Nữ</option>
                            <option value="other" <%=user.gender==='other' ? 'selected' : '' %>>Khác</option>
                        </select>
                    </div>

                    <!-- Phone number -->
                    <div class="w-full">
                        <label for="phone" class="block text-sm font-medium text-gray-700 mb-1">Số điện thoại</label>
                        <input type="tel" id="phone" name="phone" value="<%= user.phone || '' %>"
                            class="w-full border border-gray-300 rounded-lg px-3 py-2 focus:outline-none focus:ring-2 focus:ring-cellphones-500"
                            placeholder="Nhập số điện thoại (tuỳ chọn)">
                    </div>

                    <!-- Email -->
                    <div class="w-full">
                        <label for="email" class="block text-sm font-medium text-gray-700 mb-1">Email</label>
                        <input type="email" id="email" name="email" value="<%= user.email %>" readonly
                            class="w-full border border-gray-300 rounded-lg px-3 py-2 bg-gray-100 text-gray-600 cursor-not-allowed">
                    </div>

                    <!-- Action button -->
                    <div class="pt-3 w-full flex justify-end gap-3 border-gray-200 cursor-pointer">
                        <button type="button" id="btn-close-edit-profile"
                            class="px-4 py-2 rounded-lg bg-gray-200 hover:bg-gray-300 text-gray-700">Huỷ</button>
                        <button type="submit"
                            class="px-4 py-2 rounded-lg bg-cellphones-500 hover:bg-cellphones-700 text-white cursor-pointer">Lưu
                            thay
                            đổi</button>
                    </div>
                </form>

            </div>
        </div>

        <!-- Dialog Add Address -->
        <div id="dialog-add-address" class="fixed inset-0 z-50 overflow-y-auto hidden">
            <!-- Backdrop -->
            <div id="backdrop-add-address" class="absolute inset-0 bg-black opacity-20 transition-opacity"></div>

            <!-- Content -->
            <div
                class="py-4 px-10 absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 flex flex-col items-start justify-start gap-5 min-h-fit min-w-[400px] md:min-w-[500px] xl:min-w-[650px] bg-white rounded-2xl">
                <!-- Header -->
                <div class="p-4 w-full">
                    <h3 id="dialogTitle" class="text-xl text-center font-semibold text-gray-800">Thêm địa chỉ
                    </h3>
                </div>

                <!-- Body -->
                <form action="/address" method="POST"
                    class="w-full space-y-5 flex flex-col justify-start items-start">
                    <div class="w-full">
                        <label for="province" class="block text-sm font-medium">Tỉnh / Thành phố<span
                                class="text-red-500">*</span></label>
                        <select id="province" name="city" class="w-full border border-gray-300 rounded-lg px-3 py-2 focus:outline-none focus:ring-2 focus:ring-cellphones-500" required></select>
                    </div>

                    <div class="w-full">
                        <label for="district" class="block text-sm font-medium">Quận / Huyện<span
                                class="text-red-500">*</span></label>
                        <select id="district" name="district" class="w-full border border-gray-300 rounded-lg px-3 py-2 focus:outline-none focus:ring-2 focus:ring-cellphones-500" disabled required></select>
                    </div>

                    <div class="w-full">
                        <label for="ward" class="block text-sm font-medium">Phường / Xã<span
                                class="text-red-500">*</span></label>
                        <select id="ward" name="ward" class="w-full border border-gray-300 rounded-lg px-3 py-2 focus:outline-none focus:ring-2 focus:ring-cellphones-500" disabled required></select>
                    </div>

                    <div class="w-full">
                        <label for="street" class="block text-sm font-medium">Địa chỉ chi tiết<span
                                class="text-red-500">*</span></label>
                        <input id="street" name="street" type="text" class="w-full border border-gray-300 rounded-lg px-3 py-2 focus:outline-none focus:ring-2 focus:ring-cellphones-500" required
                            placeholder="Ví dụ: 123 Nguyễn Văn Cừ">
                    </div>

                    <hr class="border-[1px] border-gray-300 w-full">

                    <!-- Name -->
                    <div class="w-full">
                        <label for="name" class="block text-sm font-medium text-gray-700 mb-1">Tên người nhận:<span
                                class="text-red-500">*</span></label>
                        <input type="text" id="name" name="recipient_name" required
                            class="w-full border border-gray-300 rounded-lg px-3 py-2 focus:outline-none focus:ring-2 focus:ring-cellphones-500"
                            placeholder="Nhập họ và tên của người nhận">
                    </div>

                    <!-- Phone -->
                    <div class="w-full">
                        <label for="name" class="block text-sm font-medium text-gray-700 mb-1">Số điện thoại:<span
                                class="text-red-500">*</span></label>
                        <input type="text" id="name" name="recipient_phone" required
                            class="w-full border border-gray-300 rounded-lg px-3 py-2 focus:outline-none focus:ring-2 focus:ring-cellphones-500"
                            placeholder="Nhập số điện thoại của người nhận">
                    </div>

                    <!-- Set as Default Address -->
                    <div class="flex items-center gap-2">
                        <input type="checkbox" id="is_default" name="is_default"
                            class="w-4 h-4 text-indigo-600 border-gray-300 rounded focus:ring-cellphones-500">
                        <label for="is_default" class="text-sm text-gray-700 cursor-pointer">
                            Đặt làm địa chỉ mặc định
                        </label>
                    </div>

                    <!-- Action button -->
                    <div class="pt-3 w-full flex justify-end gap-3 border-gray-200 cursor-pointer">
                        <button type="button" id="btn-close-add-address"
                            class="px-4 py-2 rounded-lg bg-gray-200 hover:bg-gray-300 text-gray-700 cursor-pointer">Huỷ</button>
                        <button type="submit"
                            class="px-4 py-2 rounded-lg bg-cellphones-500 hover:bg-cellphones-700 text-white cursor-pointer">Lưu
                            thay đổi</button>
                    </div>
                </form>
            </div>
        </div>

        <%- include("./partials/footer.ejs") %>

        <script>
            const user = <%- JSON.stringify(user) %>;          
            const addresses = <%- JSON.stringify(addresses) %>;          
        </script>
        <script src="/js/userInfo.js"></script>
</body>

</html>