<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>LAPTOPSHOP - Đăng nhập</title>
    <link rel="stylesheet" href="/stylesheets/output.css" />
    <link rel="stylesheet" href="/stylesheets/slider.css" />
</head>

<body class="min-h-screen bg-cover bg-center" style="background-image: url('/asset/images/backgrounds/login.jpg');">
    <main class="min-h-screen min-w-full flex justify-center items-center">
        <div
            class="py-7 px-10 lg:px-20 md:min-w-[75%] lg:min-w-1/3 min-w-[90%] bg-white text-black rounded-2xl shadow-lg opacity-95">
            <h1 class="text-3xl font-bold mb-6 text-center">Đăng nhập</h1>
            <% if (typeof errors !== 'undefined' && errors.length > 0) { %>
                <div class="mb-4">
                    <ul class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded">
                        <% errors.forEach(function(error) { %>
                            <li><%= error %></li>
                        <% }); %>
                    </ul>
                </div>
            <% } %>
            <form action="login" method="POST" class="space-y-6">
                <div>
                    <label for="email" class="block text-sm font-medium mb-2">Email</label>
                    <input value="<%= typeof old !== 'undefined' && old.email ? old.email : '' %>" type="text" id="email" name="email" required
                        class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-cellphones-500" />
                </div>
                <div>
                    <label for="password" class="block text-sm font-medium mb-2">Mật khẩu</label>
                    <input type="password" id="password" name="password" required
                        class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-cellphones-500" />
                </div>
                <div class="flex justify-between items-center">
                    <a href="forgot-password" class="ml-auto text-sm text-cellphones-600 hover:underline">Quên mật khẩu?</a>
                </div>
                <button
                    class="w-full bg-gradient-to-b from-cellphones-300 to-cellphones-500 hover:from-cellphones-600 hover:to-cellphones-800 text-white py-2 rounded-md transition duration-300 cursor-pointer"
                    type="submit">
                    Đăng nhập
                </button>
            </form>
            <div class="mt-6 text-center">
                <span class="text-sm">Chưa có tài khoản?</span>
                <a href="register"
                    class="ml-2 inline-block bg-cellphones-500 hover:bg-cellphones-600 text-white px-4 py-2 rounded-md font-semibold transition duration-300">Đăng
                    ký</a>
            </div>
        </div>
    </main>
</body>

</html>